# GitHub Push Checklist

- [x] `git init` done in `prepWise/`
- [x] `.gitignore` created (covers `.env.local`, `.env.prod`, `__pycache__`, `*.pyc`, `venv/`, `.DS_Store`)
- [x] `.env.local` and `.env.prod` are in `.gitignore` — confirm they are NOT tracked
- [x] `.env.example` committed (all keys, empty values)
- [x] No secrets in any tracked file
- [x] `README.md` reviewed and accurate
- [x] Initial commit created
- [x] GitHub repo created (`prepwise`)
- [x] Remote added (`git remote add origin <url>`)
- [x] Pushed to `main`
- [x] `develop` branch created and pushed
